class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        vector<int> solution;
        unordered_map<int,int> map;
        int size = nums.size();

        for(int i=0;i<size;i++){
            int first  = nums[i];
            int second = target - first;

            if(map.find(second) != map.end()){
                solution.push_back(i);
                solution.push_back(map[second]);
                break;
            }
            map[first] = i;
        }
         return solution;
    }
};
